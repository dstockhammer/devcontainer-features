{
  "name": "Dan's zsh config",
  "id": "zsh",
  "version": "1.4.0",
  "description": "Sets up zsh as default shell with [Dan's zsh config](https://github.com/dstockhammer/zsh).",
  "options": {
    "failOnError": {
      "type": "boolean",
      "description": "Whether to fail the container build if the feature fails to install.",
      "default": false
    },
    "zshHistory": {
      "type": "string",
      "description": "The location of the .zsh_history file. Leave empty for default in Docker volume.",
      "default": ""
    }
  },
  "mounts": [
    {
      "source": "zsh",
      "target": "/var/zsh",
      "type": "volume"
    }
  ],
  "dependsOn": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "installOhMyZsh": false,
      "installOhMyZshConfig": false
    },
    "ghcr.io/devcontainers-extra/features/starship:1": {}
  }
}
